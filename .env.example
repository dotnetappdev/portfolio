# Copy this file to .env and fill in your values before running docker compose.
# The .env file is excluded from version control (see .gitignore).
#
# QUICK START:
#   cp .env.example .env
#   # Edit .env and set JWT_KEY to a strong random string (min 32 chars)
#   docker compose up --build

# REQUIRED: JWT signing key — must be at least 32 characters long.
# Generate one: openssl rand -base64 32
JWT_KEY=your-secret-key-minimum-32-characters-long-REPLACE-THIS

# Admin account seeded on first run (change before going live).
ADMIN_EMAIL=admin@portfolio.dotnetappdevni.com
ADMIN_PASSWORD=Admin@123456!

# CORS allowed origin for the API (should match the web app's public URL).
ALLOWED_ORIGINS=http://localhost:5072

# Base URL for the Portfolio API (used by the web app to call the API).
BASE_API_URL=http://portfolio-api:8080/
